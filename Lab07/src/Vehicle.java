/*
 * An interface is an abstract class where all methods are prototypes. 
 * Means all methods are have header definition with no code.
 */

/**
 *
 * @author George
 */
public interface Vehicle {
    void move(int time);
}
